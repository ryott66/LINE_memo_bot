## 📘 LINE 便利メモ Bot
LINE × GAS × スプレッドシート
URL／メモを保存・表示・削除できる LINE Bot

---

### 🚀 プロダクト概要
このLINE公式アカウントを友だち追加すると、**トーク画面下部の「リッチメニュー」から直感的に操作できます。**

提供機能は下記の通り：
| **機能カテゴリ** | **操作** | **説明** |
| --- | --- | --- |
| **URL** | **表示** | **保存したURLを一覧で返信** |
| **URL** | **記録** | **入力されたURLを保存** |
| **URL** | **削除** | **指定番号のURLを削除** |
| **メモ** | **表示** | **保存したテキストメモを一覧で返信** |
| **メモ** | **記録** | **テキストメモを保存** |
| **メモ** | **削除** | **指定番号のメモを削除** |

---

## 💻 システム構成
```
  LINE
   ↓（Webhook送信）
【Cloud Run Functions（署名検証・セキュリティ境界）】
   ↓（relay: base64 + HMAC）
【GAS Web App（ビジネスロジック）】
   ↓
Spreadsheet（データ保存）
   ↓
  GAS
   ↓
LINE Messaging API（返信）
```
1. ユーザーが Bot にメッセージ
2. webhook が Cloud Run Functions (CRF) に送られる
3. CRFでLINEからのリクエストであることをキーで認証 ⇒ GASにリクエストをリレー
4. GASがリレーを認証し、スプシと接続してメモの保存/削除を処理
5. GAS がAPIを叩いてメッセージを返信

---

### 📁 フォルダ構成
```
Project
├─ .clasp.json        # clasp 設定
├─ .clasprc.json      # clasp 認証
├─ .gitignore
├─ appsscript.json    # GAS
├─ main.js            # メインロジック
├─ utils.js           # 共通処理
├─ test.js            # テスト
└─ README.md
```
### 💫 GAS開発フロー
```
┌────────┐     push       ┌──────────────┐
│        │ ────────────→  │              │
│ VSCode │     clasp      │  GAS (本番)  │
│ (local)│ ←────────────  │              │
└────────┘     pull       └──────────────┘
     │
     │ git
     ↓
┌──────────┐
│  GitHub  │
└──────────┘
```
---


